# Information Disclosure — Other systems (suggestions)

- Logs/observability: PII redaction tests, sampling policies, access control for dashboards.
- Object storage: bucket policy tests, public access blocks, signed URL expiry checks.
- LLM/AI systems: data leakage tests (secrets in context, unintended memorization), tool output redaction.


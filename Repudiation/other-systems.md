# Repudiation — Other systems (suggestions)

- Serverless: ensure function invocations are traceable with request IDs and principal identity.
- Data pipelines: lineage tracking (source → transform → sink) and job-run audit events.
- Kubernetes: audit logs for `kubectl exec`, secret reads, and RBAC changes.

